# ===========================================
# CAR INVENTORY BACKEND - ENVIRONMENT CONFIG
# ===========================================

# Server Configuration
PORT=5000
HOST=0.0.0.0
NODE_ENV=development

# Google Sheets Configuration
GOOGLE_SHEETS_CREDENTIALS_PATH=./credentials/google-credentials.json
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id_here
GOOGLE_APPLICATION_CREDENTIALS=./credentials/google-credentials.json

# Auth0 Configuration
AUTH0_DOMAIN=your-domain.auth0.com
AUTH0_AUDIENCE=your-api-identifier
AUTH0_ISSUER=https://your-domain.auth0.com/

# Rate Limiting (Development: 2000, Production: 100)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=2000

# CORS Configuration
FRONTEND_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# Logging Configuration
LOG_LEVEL=debug

# ===========================================
# PRODUCTION OVERRIDES
# ===========================================
# Uncomment and modify these for production:

# NODE_ENV=production
# PORT=5000
# HOST=0.0.0.0

# AUTH0_DOMAIN=your-production-domain.auth0.com
# AUTH0_AUDIENCE=https://your-production-api-identifier
# AUTH0_ISSUER=https://your-production-domain.auth0.com/

# RATE_LIMIT_MAX_REQUESTS=100

# CORS_ORIGIN=https://your-frontend-domain.com
# FRONTEND_URL=https://your-frontend-domain.com

# LOG_LEVEL=info

# ===========================================
# SECURITY NOTES
# ===========================================
# 1. Never commit .env files to version control
# 2. Use strong, unique values for all secrets
# 3. Restrict file permissions: chmod 600 .env
# 4. Use environment-specific Auth0 applications
# 5. Regularly rotate Google service account keys
# 6. Monitor API usage and set up alerts
